module.exports = (req, res, next) => {

	// what questions has the user NOT answered lately?

	//

	// send back game info, opponent info...
	res.send({
		opponent: {
			name: 'dude',
			level: 4
		}
	})
};